import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4f6ef7",
    },
    secondary: {
      main: "#000000",
    },
    error: {
      main: "#808080",
    },
    info: {
      main: "#DDE2FC",
    },
  },
});

export default theme;
